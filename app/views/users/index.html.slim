- title 'User Listing'

.panel.panel-default
  .panel-heading
    strong Users

    ul.options.pull-right.list-unstyled.relative
      li: a.btn.btn-primary.btn-xs href=new_user_path
        i.fa.fa-plus
        | Add

  .panel-body.nopadding
    table.table
      thead
        tr
          th Display Name
      tbody
        - if @users.any?
          - @users.each do |user|
            tr
              td= link_to locale.display_name, user
        - else
          tr
            td colspan="2" No Users